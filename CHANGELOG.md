# Changelog

## [Unreleased]

### Added
- 输入框（composer 卡片）新增焦点样式：获得焦点时描边转为 60% 不透明度的陶土色细线，外扩 1px 暖色晕边并加深投影，形成发光感；输入区下方的底栏托盘同步跟随，整体轮廓保持一体。深色主题描边 64% 不透明度、晕边更强、环境阴影更深。
- 模型思考状态重塑：完整收录 Claude Code 的 185 种思考模式动词（如 `Smooshing...`、`Boogieing...`、`Clauding...`、`Noodling...`、`Thinking...` 等），每会话每轮思考时随机抽取一个动词稳定展示（非跑马灯轮播）；视觉上将原本的 DeepSeek 蓝渐变扫光替换为陶土橙强调色（`#D97757`）与蜜桃暖色高光的流动质感。
- 工作区运行中状态（loading）重塑：对话在运行时，工作区侧栏会话状态移除 DeepSeek 原生的方形蓝色 LED 矩阵加载器，替换为 Windows 11 Fluent 风格的灰白色圆形加载动画（微透底轨与优雅旋转圆弧），深浅主题自适应。
- 对话内输入框单行压缩与工具栏布局重组：在已有对话中，输入框压缩为紧凑的单行卡片，右侧内置回车符号（`↵`）发送按钮（回车直接发消息，Shift+Enter 换行，支持中文等 IME 正常选词输入）；权限控制组件移至输入框左下方并左对齐，采用 `position: fixed` + `document.body` 挂载彻底消除父级剪裁，实现流畅的浮层弹出菜单（支持 `Read only` / `Accept edits` / `Full access` 快捷切换与勾选）；`+` 号及回形针附件按钮排列在权限组件右侧；模型选择器移至右下角；新会话（Hero）保持经典双层分段设计。
- 对话内输入框交互与下置控件优化：下置控件（权限组件、工具按钮、模型选择器）与已有的 Token 用量及耗时指示器（`[data-composer-stats]`）合并至同一行呈现，采用 Flex 容器自适应排列彻底避免重叠碰撞；修复点击对话框空白处无法聚焦与 Hint 占位符丢失问题（对话内呈现 `Type / for commands`）；保留输入框自由纵向伸缩能力，单行起步（38px）、多行自然向下撑高并支持平滑滚动。
- 修复对话内输入框自适应宽度与横向滚动条问题：明确定义输入卡片、内部文本层与底部工具栏宽度自适应机制（`width: 100%` 与 `box-sizing: border-box`），添加 `overflow-x: hidden` 杜绝 Windows 系统及缩放比例下的异常左右滚动条。
- 彻底解决权限控制组件反复挂载与多实例残留问题：重构 `syncSegments` 挂载逻辑，加入全局单例检查与自动清理机制，确保 DOM 中始终仅存在一个权限触发按钮。
- 图片与附件输入自适应撑高：修复附件状态检测规则，仅在真实图片/文件缩略图（`imageItem` / `thumbnail` / `FileCard`）存在时才激活附件模式；空状态下附件轨完全隐藏且输入框保持完整四周圆角与单行高度（38px），有附件时附件轨与输入框平滑连接为一体化卡片并撑高文本区。
- **可切换品牌标识**：设置页新增「Claude Style」分区（`settings.section` 槽，order 22），用分段控件在 Claude 与 Anthropic 两套品牌标识间切换，默认 Claude。侧栏改用 Claude 官方星芒 + Claude 字标（星芒与字标各为独立遮罩，便于分别定尺寸），新会话页品牌标识随所选品牌切换并保持陶烬橙填充。

### Changed
- 品牌切换走 `data-dsh-claude-brand` 文档属性：样式表按属性分支，切换只写一个属性，无需重建样式。
- 品牌选择持久化在渲染进程 localStorage（`dsh-claude-style:brand`），重启应用后保持；读写均做容错，存储不可用时回退默认。
- 设置分区注册改为声明式等待：`ctx.inject(['slots'])` 等槽服务、`slots.inject('settings.section')` 等槽声明，避免 `apply` 期服务未就绪时静默丢失入口。

## [0.2.1] - 2026-09-19

### Added
- 账户抽屉支持悬停展开与延迟关闭，移入弹层不打断浏览。

### Changed
- 权限控制器首段从 Plan 改名为 Read，与它映射的只读预设（read-only）名称一致。
- 新会话按钮改为与会话行同高的窄条：左对齐加号图标、常驻悬停底色。
- 会话行标题默认为次级灰，悬停或选中时回到主文字色。
- 账户底栏改为贯通侧栏的全宽分割线布局。

### Fixed
- 补齐 LICENSE 版权署名；修正 README 中无效的手工 patch 配置示例。

## [0.2.0] - 2026-09-19

### Added
- **Claude Code Desktop Theme**: 重构，从配色皮肤升级为完整的 Claude Code Desktop 视觉与交互复刻主题。
- **Plan / Edit / Auto 分段权限控制**：行内三段式控制器，支持快捷切换会话权限（Plan 只读、Edit 写入、Auto 完全权限并带安全确认）。
- **侧栏账户抽屉**：侧栏底部集成账户按钮与弹出菜单，支持一键打开设置（`Ctrl+,`）与管理插件。
- **视觉与文案重塑**：专属问候语、输入框引导文案与品牌星芒标识。

### Changed
- 项目重命名为 `dsh-claude-style`，与上游 `claude-style-skin` 区分。
- 优化亮色画布色值（`#FCFCFB`）与侧栏色值（`#FBFBF9`）。

## [0.1.0] - 2026-08-22

### Added
- 初始版本：暖调象牙白/暖黑双画布与陶烬橙强调色。
